# Database migrations

Generated Drizzle migrations live here.

Use `npm run db:generate` after changing `lib/db/schema.ts`, then use `npm run db:migrate` to apply committed migrations to the database configured by `DATABASE_URL`.
